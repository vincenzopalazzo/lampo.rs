[workspace]
members = [
        "lampo-common",
        "lampod",
        "lampod-cli",
        "lampo-cli",
        "lampo-httpd",
        "lampo-lnd",
        "lampo-testing",
        "tests/tests",
        "lampo-chain",
        "lampo-bdk-wallet"
]

default-members = [
        "lampo-common",
        "lampod",
        "lampod-cli",
        "lampo-cli",
        "lampo-httpd",
        "lampo-lnd",
        "lampo-chain",
        "lampo-bdk-wallet"
]
resolver = "2"

# LDK 0.3 is published to crates.io as a pre-release, so the requirement has to
# spell out `0.3.0-rc2`: a bare `0.3` does not match pre-release versions.
# Bump every lightning* crate together so the stack stays on one version
# (see issue #537).
[workspace.dependencies]
lightning = "0.3.0-rc2"
lightning-block-sync = "0.3.0-rc2"
lightning-persister = { version = "0.3.0-rc2", features = ["tokio"] }
lightning-background-processor = "0.3.0-rc2"
lightning-net-tokio = "0.3.0-rc2"
lightning-rapid-gossip-sync = "0.3.0-rc2"
